{
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": ""
  },
  "scripts": {
  "post-install-cmd": [
    "@php artisan migrate --force",
    "@php artisan session:table"
  ]
}

}
